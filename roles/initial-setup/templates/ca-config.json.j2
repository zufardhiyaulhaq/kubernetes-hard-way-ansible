{
  "signing": {
    "default": {
      "expiry": "{{ infrastructure.ca.expireTime }}"
    },
    "profiles": {
      "kubernetes": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "{{ infrastructure.ca.expireTime }}"
      }
    }
  }
}
