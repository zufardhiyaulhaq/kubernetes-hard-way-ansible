kind: EncryptionConfig
apiVersion: v1
resources:
  - resources:
{% for resource in kubernetes.components.encryptionProvider.resources %}
      - {{ resource }}
{% endfor %}
    providers:
      - aescbc:
          keys:
            - name: primary
              secret: {{ kubernetes.components.encryptionProvider.key }}
      - identity: {}
